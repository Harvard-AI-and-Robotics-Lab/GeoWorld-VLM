Place dataset split files here:

- `data_split.json` for WhatsUp + VSR style datasets.
- `embspatial_split.json` for EmbSpatial.
- `sat_split.json` for SAT.

These files are intentionally not included in this clean repository skeleton.

